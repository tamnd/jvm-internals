# BP-HEADER section 2. Data structures

Generated by `tools/gen_section2.py`. Do not edit it, edit the measurement. It has three sources and transcribes none of them: the struct layouts come out of a running VM's Serviceability Agent type database in `probes/sa/results`, the mark word's bits come from `src/hotspot/share/oops/markWord.hpp` by way of `docs/generated/markword.json`, and the configuration they are contingent on comes from `probes/capability/results`. Everything here is HotSpot at `jdk-27+35` and none of it is specified by the JVMS.

## 2.1 The configuration these layouts describe

Every layout below is contingent on these, measured on 4 environments by `probes/capability/run.py`. A reader who changes one of them is reading a different section 2.

| setting | measured | what it decides |
|---|---|---|
| `vm.UseCompactObjectHeaders` | `yes` on `linux-x64-root`, `linux-x64-user`, `osx-arm64`, `win-x64` | whether the class pointer lives in the mark word or in a field after it |
| `vm.UseCompressedOops` | `yes` on `linux-x64-root`, `linux-x64-user`, `osx-arm64`, `win-x64` | whether a reference stored in a field is 4 bytes or 8 |
| `flag.UseCompressedClassPointers` | `removed` on `linux-x64-root`, `linux-x64-user`, `osx-arm64`, `win-x64` | the flag that used to decide the class pointer width |
| `vm.ObjectAlignmentInBytes` | `8` on `linux-x64-root`, `linux-x64-user`, `osx-arm64`, `win-x64` | the boundary every object size is rounded up to |
| `vm.arch` | `aarch64` on `osx-arm64`; `amd64` on `linux-x64-root`, `linux-x64-user`, `win-x64` | the architecture, which decides the word size |

`UseCompressedClassPointers` reads `removed` because the flag no longer exists in this release: the VM accepts it, warns, and ignores it. A check that reads the exit status of a VM started with it will report that it worked.

## 2.2 The mark word

64 bits, in one machine word, at offset 0 of every object. The type database has `markWord` as a type of 8 bytes and exports no fields of it, so this table is parsed from the header rather than read out of a VM, and every row carries the line it came from.

| bits | width | mask | field | meaning | defined at |
|---|---|---|---|---|---|
| 0..1 | 2 | `0x0000000000000003` | `lock` | the lock state, and whether a collector has marked or forwarded the object | src/hotspot/share/oops/markWord.hpp:124@jdk-27+35 |
| 2 | 1 | `0x0000000000000004` | `self_fwd` | set when a collector forwarded the object in place | src/hotspot/share/oops/markWord.hpp:125@jdk-27+35 |
| 3..6 | 4 | `0x0000000000000078` | `age` | how many collections the object has survived | src/hotspot/share/oops/markWord.hpp:126@jdk-27+35 |
| 7..10 | 4 | `0x0000000000000780` | `valhalla` | reserved for Valhalla, unused today | src/hotspot/share/oops/markWord.hpp:127@jdk-27+35 |
| 11..41 | 31 | `0x000003fffffff800` | `hash` | the identity hash, written the first time anything asks for it | src/hotspot/share/oops/markWord.hpp:128@jdk-27+35 |
| 42..63 | 22 | `0xfffffc0000000000` | `klass` | the compressed class pointer, present only when UseCompactObjectHeaders is on | src/hotspot/share/oops/markWord.hpp:150@jdk-27+35 |

The two low bits are a state, and the rest of the word means what those two bits say it means.

| `lock` | state | meaning |
|---|---|---|
| `00` | locked | a stack lock is held, and the real header is in the lock record |
| `01` | unlocked | the ordinary state |
| `10` | monitor | the lock is inflated |
| `11` | marked | a collector is using the word, and the real header is elsewhere |

The klass field is present only when UseCompactObjectHeaders is on, which is the default on 64 bit platforms from JDK 27. With the flag off, those 22 bits are unused in the mark word and the class pointer is a separate 4 byte field after it. The C++ oopDesc struct declares that field either way, so reading the struct alone will tell you the object has one when it does not.

## 2.3 The structs

As the type database resolved them, read by `linux-x64-root` and `linux-x64`. An offset is bytes from the start of the struct. A static field has an address rather than an offset and is printed as `static`.

### 2.3.1 markWord

`markWord`, 8 bytes, no superclass, 0 exported fields.

The type is exported and not one of its fields is, which is why 2.2 is read from the header instead.

### 2.3.2 oopDesc

`oopDesc`, 16 bytes, no superclass, 2 exported fields.

| offset | field | type |
|---|---|---|
| 0 | `_mark` | `markWord` |
| 8 | `_compressed_klass` | `narrowKlass` |

### 2.3.3 Klass

`Klass`, 200 bytes, extends `Metadata`, 13 exported fields.

| offset | field | type |
|---|---|---|
| 8 | `_layout_helper` | `jint` |
| 16 | `_super_check_offset` | `juint` |
| 24 | `_name` | `Symbol*` |
| 32 | `_secondary_super_cache` | `Klass*` |
| 40 | `_secondary_supers` | `Array<Klass*>*` |
| 48 | `_primary_supers[0]` | `Klass*` |
| 112 | `_java_mirror` | `OopHandle` |
| 120 | `_super` | `Klass*` |
| 128 | `_subklass` | `Klass*` |
| 136 | `_next_sibling` | `Klass*` |
| 144 | `_next_link` | `Klass*` |
| 152 | `_class_loader_data` | `ClassLoaderData*` |
| 184 | `_vtable_len` | `int` |

### 2.3.4 InstanceKlass

`InstanceKlass`, 472 bytes, extends `Klass`, 29 exported fields.

| offset | field | type |
|---|---|---|
| 200 | `_annotations` | `Annotations*` |
| 216 | `_array_klasses` | `ObjArrayKlass*` |
| 224 | `_constants` | `ConstantPool*` |
| 232 | `_inner_classes` | `Array<jushort>*` |
| 240 | `_nest_members` | `Array<jushort>*` |
| 272 | `_source_debug_extension` | `char*` |
| 280 | `_nonstatic_field_size` | `int` |
| 284 | `_static_field_size` | `int` |
| 288 | `_nonstatic_oop_map_size` | `int` |
| 292 | `_itable_len` | `int` |
| 296 | `_nest_host_index` | `u2` |
| 300 | `_static_oop_field_count` | `u2` |
| 302 | `_idnum_allocated_count` | `u2` |
| 304 | `_init_state` | `InstanceKlass::ClassState` |
| 305 | `_reference_type` | `u1` |
| 306 | `_access_flags` | `AccessFlags` |
| 312 | `_init_thread` | `JavaThread*` |
| 320 | `_oop_map_cache` | `OopMapCache*` |
| 328 | `_jni_ids` | `JNIid*` |
| 336 | `_methods_jmethod_ids` | `jmethodID*` |
| 360 | `_osr_nmethods_head` | `nmethod*` |
| 368 | `_breakpoints` | `BreakpointInfo*` |
| 400 | `_methods` | `Array<Method*>*` |
| 408 | `_default_methods` | `Array<Method*>*` |
| 416 | `_local_interfaces` | `Array<InstanceKlass*>*` |
| 424 | `_transitive_interfaces` | `Array<InstanceKlass*>*` |
| 432 | `_method_ordering` | `Array<int>*` |
| 440 | `_default_vtable_indices` | `Array<int>*` |
| 448 | `_fieldinfo_stream` | `Array<u1>*` |

## 2.4 What these tables do not say

The type database exports what `vmStructs` chose to export, which is less than the struct. Nothing in it distinguishes a field that is wide from a field that was not exported, so the ground between consecutive exported offsets is listed here and interpreted nowhere.

| type | from | to | bytes |
|---|---|---|---|
| `Klass` | 48 | 112 | 64 |
| `Klass` | 152 | 184 | 32 |
| `Klass` | 184 | 200 | 16 |
| `InstanceKlass` | 200 | 216 | 16 |
| `InstanceKlass` | 240 | 272 | 32 |
| `InstanceKlass` | 336 | 360 | 24 |
| `InstanceKlass` | 368 | 400 | 32 |
| `InstanceKlass` | 448 | 472 | 24 |

Two of those rows have an explanation the database does not carry. `Klass._primary_supers[0]` is the first element of an array the database names by its first element and gives no length for, and the next exported field is 64 bytes later, which is eight pointers. The last row of each type runs from its last exported field to its size, and for `Klass` that ground holds the vtable length's neighbours rather than nothing.

`Klass` starts at 8 rather than 0 for a different reason: it extends `Metadata`, which this section did not ask the probe for, so the first 8 bytes belong to a type that is not here. The table above does not show that as a gap, because measuring a subclass from 0 would call its superclass missing.

The last thing is not a gap at all. `oopDesc` is 16 bytes here with a `_compressed_klass` field at 8, and that is the struct the compiler laid out rather than the header this VM uses: with `UseCompactObjectHeaders` measured on above, the class pointer is in the mark word and the header is one word. A section 2 that printed the struct without that sentence would tell a reader an object has a separate class pointer when it does not.
